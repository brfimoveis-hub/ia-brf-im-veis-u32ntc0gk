// Re-export of the Meta CAPI configuration panel so the legacy
// ConfiguracoesCore route can render it. The canonical implementation lives in
// src/pages/SettingsConnections/CapiPanel.tsx.
export { CapiPanel as MetaCapiConfig, CapiPanel as default } from './SettingsConnections/CapiPanel'
